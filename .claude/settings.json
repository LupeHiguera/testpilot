{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run validate > /tmp/testpilot-validate.log 2>&1 || { echo 'npm run validate FAILED — last 40 lines:' >&2; tail -40 /tmp/testpilot-validate.log >&2; exit 2; }",
            "shell": "bash",
            "timeout": 180,
            "statusMessage": "Validating (build + unit tests)…"
          }
        ]
      }
    ]
  }
}
